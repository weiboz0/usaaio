- r1-001-p09: answer: 0.6860096566841435

Methodology: one frozen, stratified 150-row validation carve (`random_state=20260804`) from the 600 labeled rows; feature effects computed on the 450-row fitting partition only; a bounded kNN-only campaign over three feature counts, two allowed scalers, Manhattan/Euclidean distance, uniform/distance voting, and seven odd `k` values. The accepted `RobustScaler` + uniform Manhattan 5-NN uses the seven strongest fitting-partition features, scores validation f1-macro `0.7896213183730716`, and is refit on all labeled rows before `predict_labels` is defined.

Grading notes: the five-clause `predict_labels` contract, kNN-only family/import fence, and clean top-to-bottom execution are hard gates; a gate failure makes the submission ineligible for scored credit. For an eligible submission, model/validation quality supplies 44 points and the summary supplies 6 points via the C10 W-A/W-B/W-C register. The exact conversion from heldout f1-macro to the performance portion of those 44 points is not specified by the problem or manifest, so `rubric-P9.md` marks that allocation explicitly rather than inventing thresholds.

Heldout audit: the deterministic 200-row split was regenerated with `python mocktests/r1-001/data/gen_p09.py --with-test`, then the locked `predict_labels` was scored once. Heldout f1-macro is `0.6860096566841435`, which is `0.1036116616889281` below validation. This expected but material underperformance is consistent with selection optimism from choosing a feature subset and hyperparameters on one carve.
